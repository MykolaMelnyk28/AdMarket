server:
  address: "localhost"
  port: 8080
  servlet:
    context-path: "/api/v1"

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/admarket_db"
    username: "root"
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
